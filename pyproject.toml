[project]
name = "quantum-transpile-audit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arda Turgut", email = "your_email@example.com" }
]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv.extra-build-dependencies]
ibm-platform-services = ["setuptools"]
